{
  "name": "trsalliance-system",
  "version": "1.0.0",
  "private": true,
  "description": "TRS Alliance v2.3 – Simulation-ready React + Tailwind platform",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "tailwindcss": "^3.3.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.24"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
{
  "name": "trs-unity-protocol",
  "version": "2.3.1",
  "scripts": {
    "setup-supabase-integration": "ts-node src/lib/supabase-integration.ts",
    "deploy-ai-boundaries": "ts-node src/lib/enhanced-ai-boundaries.ts",
    "configure-trs-deployment": "ts-node src/deploy/trs-deployment-config.ts",
    "initialize-deepseek-analyzer": "ts-node src/ai-agents/deepseek-analyzer.ts",
    "enable-firebase-sync": "ts-node src/lib/firebase-sync.ts",
    "validate-system-health": "ts-node src/deploy/system-validator.ts",
    "deploy-production": "ts-node src/deploy/deploy-guardian.ts"
  }
}
